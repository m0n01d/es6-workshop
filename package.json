{
  "name": "es6-workshop",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npm run browser & npm run watch",
    "browser": "browser-sync start --browser \"google chrome\" --server --ss public --files public/*",
    "watch": "watchify ./app/main.js -o public/bundle.js"
  },
  "author": "Dwight Doane",
  "license": "ISC",
  "devDependencies": {
    "babelify": "^7.3.0",
    "browser-sync": "^2.15.0",
    "browserify": "^13.1.0",
    "watchify": "^3.7.0"
  }
}
